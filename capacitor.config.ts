import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.xaurface',
  appName: 'Xaur Face Attendance',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
